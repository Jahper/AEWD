<?xml version="1.0"?>
<robot xmlns:xacro="http://ros.org/wiki/xacro">

  <xacro:macro name="ublox" params="parent_link ublox_link_name joint_origin_xyz joint_origin_rpy">

    <link name="${ublox_link_name}"/>

    <joint name="${ublox_link_name}_joint" type="fixed">
      <origin xyz="${joint_origin_xyz}" rpy="${joint_origin_rpy}"/>
      <parent link="${parent_link}"/>
      <child link="${ublox_link_name}"/>
    </joint>

  </xacro:macro>

</robot>